name: nl2sql_tool
description: "自然语言到SQL查询转换器，支持查询交易流水、机构信息、商户数据和账户信息。表结构：1.交易流水表(transaction_flow)：transaction_id(交易流水ID),merchant_id(商户ID),institution_id(机构ID),account_id(付款方账户ID),counterparty_id(收款方账户ID),transaction_amount(交易金额),transaction_type(交易类型),transaction_time(交易时间),status(交易状态),remark(备注)。2.机构信息表(institution_info)：institution_id(机构ID),institution_name(机构名称),institution_type(机构类型),license_no(金融许可证号),legal_entity(法人代表),contact_phone(电话),contact_email(邮箱),status(状态),create_time(创建时间)。3.商户信息表(merchant_info)：merchant_id(商户ID),merchant_name(商户名称),merchant_type(商户类型),merchant_category(商户类别码MCC),legal_person(法人姓名),business_license(营业执照号),settlement_account(结算账户ID),status(状态),register_time(注册时间)。4.账户信息表(account_info)：account_id(账户ID),account_name(账户名称),account_type(账户类型),bank_name(开户银行),bank_account_no(银行账号),status(状态),create_time(创建时间)。支持单表查询和多表关联查询，自动识别查询条件并生成SQL语句。"
parameters:
  required: []